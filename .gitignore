node_modules/
data/
config/config.yaml
config/rules-*.yaml
!config/rules-example-*.yaml
config/rules.yaml.bak
firebase-debug.log
*.log
.env
.env.*
!.env.example

# Local rule action hooks can contain personal routing, account IDs, or
# workspace-specific credentials. Keep reusable examples in docs instead.
scripts/*.sh

graphify-out/
